on: [push]

name: build

jobs:
  check:
    name: Run checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install latest nightly
        uses: actions-rs/toolchain@v1
        with:
            toolchain: nightly
            override: true
            components: rustfmt, clippy

      # `cargo check` command here will use installed `nightly`
      # as it is set as an "override" for current directory

      - name: Run cargo check
        uses: actions-rs/cargo@v1
        with:
          command: check

  build:
    name: Run tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Install latest nightly
        uses: actions-rs/toolchain@v1
        with:
            toolchain: nightly
            override: true
            components: rustfmt, clippy
      - name: Run tests
        run: cargo test --verbose

  miri-test:
    name: Test with miri
    runs-on: ubuntu-latest
    env:
      MIRIFLAGS: Mon
    steps:
      - uses: actions/checkout@v2
      - uses: hecrj/setup-rust-action@v1
        with:
          rust-version: nightly
          components: miri
      - run: MIRIFLAGS=-Zmiri-disable-isolation cargo miri test --verbose --no-default-features
      - run: MIRIFLAGS=-Zmiri-disable-isolation cargo miri test --verbose --all-features


# deactivated because: cargo-nono currently rejects the code in src/tests as not no_std, even though it should be hidden by the 'test' feature
#  ensure_no_std:
#    name: Ensure no_std
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v2
#      - uses: actions-rs/toolchain@v1
#        with:
#          toolchain: stable
#          override: true
#      - name: Download cargo-nono
#        run: |
#          wget https://github.com/hobofan/cargo-nono/releases/download/0.1.9/cargo-nono-0.1.9-x86_64-unknown-linux-gnu.tar.gz \
#            && tar xfvz cargo-nono-0.1.9-x86_64-unknown-linux-gnu.tar.gz
#      - name: Run check
#        run: ./cargo-nono check
